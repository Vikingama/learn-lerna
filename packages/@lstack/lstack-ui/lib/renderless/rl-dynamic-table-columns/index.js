import RlDynamicTableColumns from './rl-dynamic-table-columns'

RlDynamicTableColumns.install = Vue => {
  Vue.component(RlDynamicTableColumns.name, RlDynamicTableColumns)
}

export default RlDynamicTableColumns
