import EditableTableFormItem from './editable-table-form-item'

EditableTableFormItem.install = Vue => {
  Vue.component(EditableTableFormItem.name, EditableTableFormItem)
}

export default EditableTableFormItem
